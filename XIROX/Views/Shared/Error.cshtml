@model XIROX.Models.ErrorViewModel?
@{
    ViewData["Title"] = "خطا";
}
<h1>خطا</h1>

@if (!string.IsNullOrEmpty(Model?.RequestId))
{
  <p>Request ID: <code>@Model!.RequestId</code></p>
}

<p>مشکلی پیش آمد. لطفاً کمی بعد دوباره تلاش کنید.</p>